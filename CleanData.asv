z=AllDataTest;
lv = max(cellfun(@length,z));
za = z{8}(35);
for idx = 1:length(z)
    cellVal = z{idx};
  if length(cellVal) < lv
      for idy =1:length(z{idx})
        tabVal= z{idx}{idy};
          if width(tabVal) < lv
          z{idx}{idy} = za;
          z{idx}(1:length(cellVal)) = cellVal;
  
          end

      end
  end
end
